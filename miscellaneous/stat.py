from cs50 import get_int

#array = [100, 0, 50]
array = input("Number Set: ")
print(" " + array)
#array = sorted(array)


#def median():if len(array) % 2 == 0:return array[len(array) / 2] + array[(len(array) / 2) + 1] / 2else:return round(len(array) / 2)

#print(f"{str(sum(array) / len(array))} is the mean and median is the median of {array}")
